#include "ui_workflows.h"

// Define the global instances
UIWorkflow pinWorkflow = { nullptr, nullptr };
UIWorkflow cameraSelectWorkflow = { nullptr, nullptr };
UIWorkflow cameraSettingsWorkflow = { nullptr, nullptr };